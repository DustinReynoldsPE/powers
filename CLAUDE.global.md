# Powers Plugin

Structured development workflows for ticket-based projects using `tk`.

Skills: `/create-feature`, `/create-bug`, `/work-ticket`, `/finishing-branch`, `/brainstorming`, `/using-git-worktrees`, `/subagent-execution`

Tickets live in `.tickets/`. One ticket = one branch = one PR.
